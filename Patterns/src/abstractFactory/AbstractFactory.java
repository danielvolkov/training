package abstractFactory;

/**
 * Created by daniel on 21/11/16.
 *
 * Abstract Factory
 *  Goal:
 * Create interface for creating many interrelated or interdependent objects
 * without hard binding
 * --
 * using for:
 * Creating a quantity interrelated objects
 *
 * Example:
 *     - system should not depended from creation method, model and view of input objects
 *     - input interrelated objects should use together
 *     - system should configure by one from many objects, from which contains
 *     - give a lot of objects, with only their interfaces, but not realization
 */
public class AbstractFactory {
    public static void main(String[] args) {
        System.out.println(" abstract factory");

    }
}
